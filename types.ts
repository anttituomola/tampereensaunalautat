export interface Lautta {
    id: string,
    name: string,
    location: string,
    capacity: number,
    pricemin: number
    pricemax: number,
    equipment: string[]
    images: string[]
    mainImage: string
  }